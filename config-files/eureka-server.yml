# Server Properties:
server:
   port: 8081

# Eureka Properties:
eureka:
    instance:
        hostname: localhost
        prefer-ip-address: true
    client:
        register-with-eureka: false
        fetch-registry: false
#    server:
#        enable-self-preservation: false